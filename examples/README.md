# Examples

Canonical plugin examples now live in this repository so they stay aligned with
the SDK contract and helper APIs.

## Included examples

- [`minimal-python-plugin`](minimal-python-plugin): copy-and-modify starter
  plugin using FastAPI and `acmed-plugin-sdk`.
